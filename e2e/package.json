{
  "name": "e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "End-to-end suite: the system through the user's door — HTTP, WebSocket and UI.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@remote-claude/contracts": "workspace:*",
    "@types/node": "^22.18.0",
    "@types/pg": "^8.23.1",
    "@types/ws": "^8.18.1",
    "pg": "^8.23.0",
    "typescript": "^5.9.3",
    "ws": "^8.21.3"
  }
}
