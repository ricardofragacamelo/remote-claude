// Generated by `pnpm contracts:generate` from packages/contracts/schema/. Do not edit.
// `pnpm contracts:check` fails when this file and the schema disagree.

/// The protocol version this build speaks.
const int protocolVersion = 1;

/// Every frame `type` the classes below cover.
const List<String> frameTypes = <String>[
  'connection.ready',
  'connection.authenticate',
  'connection.reauthenticate',
  'error',
];

/// `kind` of a connection.ready frame.
const String connectionReadyKind = 'ack';

/// `type` of a connection.ready frame.
const String connectionReadyType = 'connection.ready';

/// `kind` of a connection.authenticate frame.
const String connectionAuthenticateKind = 'command';

/// `type` of a connection.authenticate frame.
const String connectionAuthenticateType = 'connection.authenticate';

/// `kind` of a connection.reauthenticate frame.
const String connectionReauthenticateKind = 'command';

/// `type` of a connection.reauthenticate frame.
const String connectionReauthenticateType = 'connection.reauthenticate';

/// `kind` of a error frame.
const String errorKind = 'error';

/// `type` of a error frame.
const String errorType = 'error';

/// Shape shared by every frame, in both directions. See docs/architecture/shared/05-websocket-protocol.md.
class Envelope {
  const Envelope({
    required this.v,
    required this.id,
    required this.kind,
    required this.type,
    required this.ts,
    this.traceId,
    this.correlationId,
    this.seq,
    this.payload,
  });

  /// Reads a decoded JSON map. Unknown keys are ignored, never rejected.
  factory Envelope.fromJson(Map<String, Object?> json) => Envelope(
        v: json['v']! as int,
        id: json['id']! as String,
        kind: json['kind']! as String,
        type: json['type']! as String,
        ts: json['ts']! as String,
        traceId: json['traceId'] as String?,
        correlationId: json['correlationId'] as String?,
        seq: json['seq'] as int?,
        payload: json['payload'] as Map<String, Object?>?,
      );

  /// Protocol version. Adding an optional field or a new event does not change it; removing a field, renaming a type or changing a meaning does.
  final int v;

  /// ULID unique to this frame.
  final String id;

  /// What the frame is. `request` travels server to client and demands an answer — that is the case the whole protocol is designed around.
  final String kind;

  /// What the frame is, within its kind.
  final String type;

  /// ISO 8601 instant, in UTC.
  final String ts;

  /// Correlates this frame with the server logs of the same operation.
  final String? traceId;

  /// `id` of the frame that caused this one.
  final String? correlationId;

  /// Monotonic per session, on events only. This is what makes replay after a reconnect possible.
  final int? seq;

  /// Contents, defined per `type`.
  final Map<String, Object?>? payload;

  /// A JSON map with the absent optional fields left out.
  Map<String, Object?> toJson() {
    final Map<String, Object?> json = <String, Object?>{
      'v': v,
      'id': id,
      'kind': kind,
      'type': type,
      'ts': ts,
    };

    if (traceId != null) {
      json['traceId'] = traceId;
    }

    if (correlationId != null) {
      json['correlationId'] = correlationId;
    }

    if (seq != null) {
      json['seq'] = seq;
    }

    if (payload != null) {
      json['payload'] = payload;
    }

    return json;
  }
}

/// What this server will accept, so the client does not have to discover it by being refused.
class ConnectionReadyPayloadLimits {
  const ConnectionReadyPayloadLimits({
    required this.maxFrameBytes,
    required this.replayBufferSize,
  });

  /// Reads a decoded JSON map. Unknown keys are ignored, never rejected.
  factory ConnectionReadyPayloadLimits.fromJson(Map<String, Object?> json) => ConnectionReadyPayloadLimits(
        maxFrameBytes: json['maxFrameBytes']! as int,
        replayBufferSize: json['replayBufferSize']! as int,
      );

  final int maxFrameBytes;

  final int replayBufferSize;

  /// A JSON map with the absent optional fields left out.
  Map<String, Object?> toJson() {
    final Map<String, Object?> json = <String, Object?>{
      'maxFrameBytes': maxFrameBytes,
      'replayBufferSize': replayBufferSize,
    };

    return json;
  }
}

/// Answer to a successful handshake. An ack, not an event: it acknowledges the command that caused it.
class ConnectionReadyPayload {
  const ConnectionReadyPayload({
    required this.connectionId,
    required this.serverVersion,
    required this.limits,
  });

  /// Reads a decoded JSON map. Unknown keys are ignored, never rejected.
  factory ConnectionReadyPayload.fromJson(Map<String, Object?> json) => ConnectionReadyPayload(
        connectionId: json['connectionId']! as String,
        serverVersion: json['serverVersion']! as String,
        limits: ConnectionReadyPayloadLimits.fromJson(json['limits']! as Map<String, Object?>),
      );

  final String connectionId;

  final String serverVersion;

  /// What this server will accept, so the client does not have to discover it by being refused.
  final ConnectionReadyPayloadLimits limits;

  /// A JSON map with the absent optional fields left out.
  Map<String, Object?> toJson() {
    final Map<String, Object?> json = <String, Object?>{
      'connectionId': connectionId,
      'serverVersion': serverVersion,
      'limits': limits.toJson(),
    };

    return json;
  }
}

/// Who is connecting, for diagnostics and for the deprecation window.
class ConnectionAuthenticatePayloadClient {
  const ConnectionAuthenticatePayloadClient({
    required this.kind,
    required this.version,
  });

  /// Reads a decoded JSON map. Unknown keys are ignored, never rejected.
  factory ConnectionAuthenticatePayloadClient.fromJson(Map<String, Object?> json) => ConnectionAuthenticatePayloadClient(
        kind: json['kind']! as String,
        version: json['version']! as String,
      );

  final String kind;

  final String version;

  /// A JSON map with the absent optional fields left out.
  Map<String, Object?> toJson() {
    final Map<String, Object?> json = <String, Object?>{
      'kind': kind,
      'version': version,
    };

    return json;
  }
}

/// Opens the connection. Without it inside 5 seconds the server closes with 4401.
class ConnectionAuthenticatePayload {
  const ConnectionAuthenticatePayload({
    required this.token,
    required this.locale,
    required this.client,
  });

  /// Reads a decoded JSON map. Unknown keys are ignored, never rejected.
  factory ConnectionAuthenticatePayload.fromJson(Map<String, Object?> json) => ConnectionAuthenticatePayload(
        token: json['token']! as String,
        locale: json['locale']! as String,
        client: ConnectionAuthenticatePayloadClient.fromJson(json['client']! as Map<String, Object?>),
      );

  /// OIDC access token. In the frame body, never in the query string — a query string leaks into proxy logs.
  final String token;

  /// Language of this connection, not of the user.
  final String locale;

  /// Who is connecting, for diagnostics and for the deprecation window.
  final ConnectionAuthenticatePayloadClient client;

  /// A JSON map with the absent optional fields left out.
  Map<String, Object?> toJson() {
    final Map<String, Object?> json = <String, Object?>{
      'token': token,
      'locale': locale,
      'client': client.toJson(),
    };

    return json;
  }
}

/// Renews the credential without reopening the socket. An access token expiring on an open connection does not drop it; failing to renew within the 60 s grace period closes it with 4401.
class ConnectionReauthenticatePayload {
  const ConnectionReauthenticatePayload({
    required this.token,
  });

  /// Reads a decoded JSON map. Unknown keys are ignored, never rejected.
  factory ConnectionReauthenticatePayload.fromJson(Map<String, Object?> json) => ConnectionReauthenticatePayload(
        token: json['token']! as String,
      );

  /// The renewed OIDC access token.
  final String token;

  /// A JSON map with the absent optional fields left out.
  Map<String, Object?> toJson() {
    final Map<String, Object?> json = <String, Object?>{
      'token': token,
    };

    return json;
  }
}

class ErrorPayloadDetailsItem {
  const ErrorPayloadDetailsItem({
    required this.field,
    required this.rule,
  });

  /// Reads a decoded JSON map. Unknown keys are ignored, never rejected.
  factory ErrorPayloadDetailsItem.fromJson(Map<String, Object?> json) => ErrorPayloadDetailsItem(
        field: json['field']! as String,
        rule: json['rule']! as String,
      );

  final String field;

  final String rule;

  /// A JSON map with the absent optional fields left out.
  Map<String, Object?> toJson() {
    final Map<String, Object?> json = <String, Object?>{
      'field': field,
      'rule': rule,
    };

    return json;
  }
}

/// The error envelope, identical to the HTTP one. See docs/architecture/shared/04-errors-and-http.md.
class ErrorPayload {
  const ErrorPayload({
    required this.code,
    required this.messageKey,
    this.params,
    required this.traceId,
    this.details,
  });

  /// Reads a decoded JSON map. Unknown keys are ignored, never rejected.
  factory ErrorPayload.fromJson(Map<String, Object?> json) => ErrorPayload(
        code: json['code']! as String,
        messageKey: json['messageKey']! as String,
        params: json['params'] as Map<String, Object?>?,
        traceId: json['traceId']! as String,
        details: json['details'] == null ? null : (json['details']! as List<Object?>).map((item) => ErrorPayloadDetailsItem.fromJson(item! as Map<String, Object?>)).toList(growable: false),
      );

  /// Stable, for client logic. Changing one is a breaking change.
  final String code;

  /// For display. The client translates it — the server never sends prose.
  final String messageKey;

  /// Interpolation values for the message key.
  final Map<String, Object?>? params;

  /// Lets a user report the problem and someone find it in the log.
  final String traceId;

  /// Validation only: every invalid field, not just the first.
  final List<ErrorPayloadDetailsItem>? details;

  /// A JSON map with the absent optional fields left out.
  Map<String, Object?> toJson() {
    final Map<String, Object?> json = <String, Object?>{
      'code': code,
      'messageKey': messageKey,
      'traceId': traceId,
    };

    if (params != null) {
      json['params'] = params;
    }

    if (details != null) {
      json['details'] = details?.map((item) => item.toJson()).toList(growable: false);
    }

    return json;
  }
}
