gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# The push transport's credential file. It is configuration, per operator, and never committed:
# a build without it compiles, and the app says notifications will not arrive (D-21).
/app/google-services.json
