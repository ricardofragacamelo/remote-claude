# CLAUDE.md

Este arquivo existe apenas para garantir que o Claude Code carregue o contexto correto do
repositório. **As instruções reais vivem no `AGENTS.md`, importado abaixo.**

@AGENTS.md

Não duplique regras aqui. Toda regra nova entra no `@AGENTS.md` (ou no documento que ele
aponta), para que qualquer agente — Claude Code, VSCode ou o backend via Agent SDK — leia a
mesma fonte.
