Fixtures for the architecture test.

They are deliberate violations of the Dependency Rule, laid out like `src/` so the very rules in
`dependency-cruiser.config.mjs` can be pointed at them. A rule that has never been seen to fail is
a rule nobody knows is switched on.
