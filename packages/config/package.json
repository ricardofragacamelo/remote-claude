{
  "name": "@remote-claude/config",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "How a process refuses to start on an environment it cannot trust. Shared by the backend and the web build.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "zod": "^4.6.5"
  }
}
