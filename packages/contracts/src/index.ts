/**
 * The WebSocket protocol: the single place it is defined in TypeScript.
 *
 * Everything here is generated from `packages/contracts/schema/` — never declare a protocol type
 * anywhere else (docs/architecture/shared/07-repository-layout.md). Backend and web import this
 * package; the Flutter app reads the Dart generated from the same schema.
 */

export * from './protocol.js';
