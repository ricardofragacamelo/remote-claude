{
  "name": "@remote-claude/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The WebSocket protocol, generated from JSON Schema. Backend and web import it; mobile generates Dart from the same schema.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
